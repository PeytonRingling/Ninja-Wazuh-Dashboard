services:
  dashboard:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      # Tell the app to store the DB inside the named volume
      DB_PATH: /data/suppression_log.db
    volumes:
      - db_data:/data
    restart: unless-stopped

volumes:
  db_data:
